Follow `AGENTS.md` at the repo root as the canonical agent guide.

Cursor-specific additions:

- keep outputs concise
- prefer updating canonical docs over adding new docs
- if API surface changes, run `.\scripts\verify-openapi.ps1 -Configuration Release`
- keep subproject READMEs minimal and local
