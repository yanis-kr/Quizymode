# Acceptance criteria

The full Given/When/Then acceptance criteria for Quizymode are maintained in **[AC.md](./AC.md)**. That file is the **canonical** source of truth; update it when API contracts, authorization, or user-visible behavior change.

This path exists so conventions and tooling that reference `docs/ACCEPTANCE_CRITERIA.md` resolve to the same document family as `AC.md`.
