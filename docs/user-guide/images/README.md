# User guide screenshots

Add **PNG** or **WebP** images here so the figures in [`../README.md`](../README.md) resolve. Recommended width **about 1200–1600 px** (resize for file size). Use a consistent browser zoom (100%) and optionally hide personal data.

| File | What to capture | Suggested place to shoot |
|------|-----------------|-------------------------|
| `01-home.png` | Hero (“Build, share…”), **Explore Categories** grid, optional **Featured sets** / **Try Sample Collection** button. | `https://www.quizymode.com/` |
| `02-categories.png` | Categories page with **mode tabs** (Sets / List / Flashcards / Quiz) visible; pick a category that shows both nav and content. | `/categories` or a deeper path like `/categories/sports` |
| `03-collections.png` | Either **collections list** (`/collections`) or a **collection detail** with items and study links. | Signed in; `/collections` or `/collections/{id}/...` |
| `04-add-items-hub.png` | **Add Items** hub with category/keyword scope and links to create / bulk / upload. | Signed in; `/items/add` |
| `05-mode-switcher.png` | **Close-up** of the four mode tabs (Sets, List, Flashcards, Quiz) so labels are readable. | Crop from same screen as `02` or `06` |
| `06-flashcards.png` | **Explore / Flashcards** UI showing one card (question side or front state is fine). | `/explore/...` with an item open |
| `07-quiz.png` | **Quiz** UI (question + answer choices or input—whatever your quiz step shows). | `/quiz/...` with an item open |
| `08-items.png` | **All items** list. | `/items` |
| `09-item-detail.png` | **Single item** detail page with content visible. | `/items/{id}` |
| `10-study-guide.png` | **Study guide** page (editor or import). | Signed in; `/study-guide` or `/study-guide/import` |
| `11-nav-signed-in.png` | Top nav showing **Categories**, **Collections**, **Add Items**, and **user menu** (no need to open the menu if space is tight). | Any signed-in page |

## Optional extras (not referenced in the guide yet)

Add these if you later expand the doc:

- `12-bulk-create.png` — `/items/bulk-create`
- `13-upload-collection.png` — `/items/upload`
- `14-admin-dashboard.png` — `/admin` (admin user only)

After adding files, verify links in [`../README.md`](../README.md) on GitHub (or your viewer) render correctly.
